spring:
  application:
    name: msclientes
    
server:
  port: 0
  
eureka:
  client:
    service-url:
      defaultZone: http://cursoms-eureka-user:R0To1up4fY2E96XTjld8uhPwJ@localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

  